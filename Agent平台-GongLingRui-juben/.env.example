# =====================================================================
# ⚠️ 安全警告
# =====================================================================
# 1. 请勿将此文件直接提交到版本控制系统
# 2. 生产环境必须修改所有默认值
# 3. 所有密码和密钥必须设置为强随机值
# 4. 建议使用强密码（大小写字母+数字+特殊字符，至少16位）
# =====================================================================

# ==================== 智谱AI配置 (必需) ====================
# 获取方式: https://open.bigmodel.cn/usercenter/apikeys
# ⚠️ 生产环境必须使用真实 API Key
ZHIPU_API_KEY=your_zhipu_api_key_here

# ==================== PostgreSQL配置 (必需) ====================
# ⚠️ 生产环境必须使用强密码
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=juben
POSTGRES_USER=juben
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
POSTGRES_SSLMODE=disable
POSTGRES_POOL_MIN=1
POSTGRES_POOL_MAX=10

# ==================== OpenAI配置 (可选) ====================
# 如果使用OpenAI模型，需要配置此项
# OPENAI_API_KEY=your_openai_api_key_here

# ==================== LangSmith配置 (可选) ====================
# 用于LLM调用追踪和调试
# 获取方式: https://smith.langchain.com/
# LANGCHAIN_API_KEY=your_langchain_api_key_here
# LANGCHAIN_PROJECT=juben-drama-planner

# ==================== OpenRouter配置 (可选) ====================
# 备用LLM提供商
# OPENROUTER_API_KEY=your_openrouter_api_key_here

# ==================== 应用配置 ====================
APP_ENV=development
AUTH_ENABLED=false
DEBUG=false
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000
USER_DB_PATH=data/users.db
RATE_LIMIT_RPM=120
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=10

# ==================== 缓存策略 ====================
CACHE_L1_MAX_SIZE=1000
CACHE_L1_TTL=300
CACHE_L2_TTL=3600
CACHE_L2_PREFIX=juben:cache:
CACHE_L2_PREFIX=
CACHE_L3_ENABLED=true
CACHE_L3_TABLE=cache_store
CACHE_KEY_PREFIX=
CACHE_SERIALIZE=json

# ==================== 管理员账户配置 ====================
# ⚠️ 生产环境必须修改默认凭据
ADMIN_USERNAME=admin
ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
ADMIN_EMAIL=admin@example.com

# ==================== JWT配置 ====================
# ⚠️ 生产环境必须设置强密钥（至少32位随机字符串）
JWT_SECRET_KEY=REPLACE_WITH_RANDOM_STRING_MIN_32_CHARS

# ==================== Redis配置 ====================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ==================== Milvus配置 ====================
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_USER=
MILVUS_PASSWORD=

# ==================== MinIO配置 ====================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# ==================== Grafana配置 ====================
GRAFANA_ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# ==================== Tavily配置 (可选) ====================
# TAVILY_API_KEY=your_tavily_api_key_here

# ==================== 阿里云配置 (可选) ====================
# DASHSCOPE_API_KEY=your_dashscope_api_key_here

# ==================== 百度配置 (可选) ====================
# BAIDU_API_KEY=your_baidu_api_key_here

# ==================== OCR配置 (可选) ====================
# OCR_SPACE_ID=your_ocr_space_id_here
# OCR_ACCESS_TOKEN=your_ocr_access_token_here

# =====================================================================
# CORS 配置
# =====================================================================
# ⚠️ 生产环境必须限制允许的域名
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
ALLOWED_HEADERS=Content-Type,Authorization,X-Message-ID,X-Session-ID

# =====================================================================
# 安全配置
# =====================================================================
# HTTPS 强制（生产环境建议启用）
FORCE_HTTPS=false

# 会话超时（秒）
SESSION_TIMEOUT=3600

# 最大会话数（每用户）
MAX_SESSIONS_PER_USER=10

# CSRF 保护
CSRF_ENABLED=true

# IP 白名单（可选，逗号分隔）
IP_WHITELIST=

# =====================================================================
# 备份配置
# =====================================================================
# 数据库备份路径
BACKUP_PATH=/backups

# 备份保留天数
BACKUP_RETENTION_DAYS=30

# 自动备份间隔（小时）
BACKUP_INTERVAL_HOURS=24

# =====================================================================
# 监控配置
# =====================================================================
# Prometheus 端口
PROMETHEUS_PORT=9090

# 性能监控间隔（秒）
PERFORMANCE_MONITOR_INTERVAL=60
